# Claude Code instructions for this repo

See `AGENTS.md` for the rules on using the `unlirice` MCP server — it's
written to apply to any agent (Claude Code, Codex, Antigravity, etc.)
connected to this project, not just Claude, so the conventions live there
rather than being duplicated per tool.

See `PROJECT_NOTES.md` for the architecture and its locked-in decisions.
